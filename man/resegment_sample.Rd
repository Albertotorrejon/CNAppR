% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resegment.R
\name{resegment_sample}
\alias{resegment_sample}
\title{Re-segment Copy Number Data}
\usage{
resegment_sample(
  data,
  sample_id,
  min_length = 1e+05,
  max_dist_segm = 1e+06,
  percent_dist = 2,
  dev_btw_segs = 0.16,
  dev_tozero = 0.16,
  dev_baf = 0.1,
  low_gain = 0.2,
  normal_gain = log2(3/2),
  high_gain = log2(4/2),
  low_loss = -0.2,
  normal_loss = log2(1/2),
  high_loss = log2(0.6/2),
  chrom_percent = 0.9,
  arm_percent = 0.5,
  focal_percent_low = 0.05,
  focal_percent_medium = 0.15,
  focal_percent_high = 0.3,
  min_baf = 0.2,
  acrocentric = FALSE,
  skip_resegmentation = FALSE
)
}
\arguments{
\item{data}{Data frame with columns: ID, chr, loc.start, loc.end, seg.mean, [BAF, purity].}

\item{sample_id}{Character sample identifier to process.}

\item{min_length}{Numeric minimum segment length in bp (default 100000).}

\item{max_dist_segm}{Numeric maximum distance between segments to consider merging (bp, default 1000000).}

\item{percent_dist}{Numeric percentage-based distance threshold (default 2).}

\item{dev_btw_segs}{Numeric max deviation between segment means for merging (default 0.16).}

\item{dev_tozero}{Numeric deviation from zero threshold (default 0.16).}

\item{dev_baf}{Numeric max BAF deviation between segments (default 0.1).}

\item{low_gain}{Numeric low gain threshold in log2 (default 0.2).}

\item{normal_gain}{Numeric normal gain threshold (default log2(3/2) ≈ 0.585).}

\item{high_gain}{Numeric high gain threshold (default log2(4/2) = 1.0).}

\item{low_loss}{Numeric low loss threshold (default -0.2).}

\item{normal_loss}{Numeric normal loss threshold (default log2(1/2) ≈ -1.0).}

\item{high_loss}{Numeric high loss threshold (default log2(0.6/2) ≈ -1.737).}

\item{chrom_percent}{Numeric minimum coverage for chromosomal alterations (0-1, default 0.9).}

\item{arm_percent}{Numeric minimum coverage for arm-level alterations (0-1, default 0.5).}

\item{focal_percent_low}{Numeric focal alteration low threshold (default 0.05).}

\item{focal_percent_medium}{Numeric focal alteration medium threshold (default 0.15).}

\item{focal_percent_high}{Numeric focal alteration high threshold (default 0.3).}

\item{min_baf}{Numeric minimum BAF for CN-LOH detection (default 0.2).}

\item{acrocentric}{Logical whether to ignore p arms of chromosomes 13,14,15,21,22 (default FALSE).}

\item{skip_resegmentation}{Logical whether to skip re-segmentation (default FALSE).}
}
\value{
Data frame (re-segmented) with additional columns:
  - score, classified, type, intensity, weight, comments
}
\description{
Performs iterative re-segmentation, fusion of similar segments, and classification
of copy number alterations (CNA).
}
\details{
This is the main CNA processing pipeline:
1. Purity correction (re-centralization if purity provided)
2. Minimum length filtering
3. Iterative segment fusion (merging similar, nearby segments)
4. Classification into chromosomal, arm-level, focal, and CN-LOH events
}
\examples{
\dontrun{
  data <- read_cna_file("sample.tsv")
  result <- resegment_sample(data, sample_id = "sample_001")
}

}
