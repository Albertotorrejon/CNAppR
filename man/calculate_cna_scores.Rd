% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scoring.R
\name{calculate_cna_scores}
\alias{calculate_cna_scores}
\title{Calculate CNA Scores}
\usage{
calculate_cna_scores(resegmented_list, sample_names = NULL)
}
\arguments{
\item{resegmented_list}{List of re-segmented data frames, one per sample (output from resegment_sample()).}

\item{sample_names}{Character vector of sample IDs (optional; inferred from names if list is named).}
}
\value{
Data frame with columns: FCS, BCS, GCS and row names as sample_names.
}
\description{
Computes FCS (Focal Copy Number Score), BCS (Broad Copy Number Score),
and GCS (Global Copy Number Score) for samples.
}
\details{
Scores are calculated as:
- **FCS** = count of focal alterations
- **BCS** = count of chromosomal alterations + count of arm-level alterations
- **GCS** = (FCS - mean(FCS)) / sd(FCS) + (BCS - mean(BCS)) / sd(BCS)

If all scores are 0, normalization is skipped.
}
\examples{
\dontrun{
  reseg_list <- lapply(c("sample_1", "sample_2"), function(id) {
    resegment_sample(data, sample_id = id)
  })
  names(reseg_list) <- c("sample_1", "sample_2")

  scores <- calculate_cna_scores(reseg_list)
  head(scores)
}

}
