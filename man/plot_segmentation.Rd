% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile_plots.R
\name{plot_segmentation}
\alias{plot_segmentation}
\title{Plot Segmentation Before and After Re-segmentation}
\usage{
plot_segmentation(
  original_data,
  resegmented_data,
  sample_id = "Sample",
  cytobands = NULL,
  centromeres = NULL,
  thresholds = NULL
)
}
\arguments{
\item{original_data}{Data frame with original segments (ID, chr, loc.start, loc.end, seg.mean).}

\item{resegmented_data}{Data frame with re-segmented segments (same columns).}

\item{sample_id}{Character sample identifier (for title).}

\item{cytobands}{Level3 cytoband reference (from get_cytobands_data()).}

\item{centromeres}{Data frame with centromere positions (chr, position).}

\item{thresholds}{List or numeric vector with gain/loss thresholds:
- low_gain, medium_gain, high_gain, low_loss, medium_loss, high_loss}
}
\value{
A ggplot2 object.
}
\description{
Visualizes original segments and re-segmented results on a genome-wide plot.
}
\details{
Creates a genome-wide plot with:
- Chromosomes separated by vertical lines
- Centromeres marked with dashed lines
- Horizontal lines for gain/loss thresholds
- Segments colored by type (original=black, resegmented=green)
}
\examples{
\dontrun{
  plot <- plot_segmentation(
    original = orig_list[[1]],
    resegmented = filt_list[[1]],
    sample_id = "sample_001",
    cytobands = l3
  )
  print(plot)
}

}
