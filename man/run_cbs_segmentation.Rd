% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bam_input.R
\name{run_cbs_segmentation}
\alias{run_cbs_segmentation}
\title{CBS segmentation (Circular Binary Segmentation)}
\usage{
run_cbs_segmentation(
  qc_data,
  sample_id,
  alpha = 0.01,
  nperm = 10000L,
  sequencing_type = c("illumina", "nanopore")
)
}
\arguments{
\item{qc_data}{Data frame output from read_bam_qc().}

\item{sample_id}{Character sample identifier.}

\item{alpha}{Numeric CBS significance level (default 0.01).}

\item{nperm}{Integer number of CBS permutations (default 10000).}

\item{sequencing_type}{Character "illumina" or "nanopore". Controls
outlier smoothing: more aggressive for Nanopore long reads.}
}
\value{
Data frame with columns: ID, chr, loc.start, loc.end, seg.mean.
  Ready to pass directly to resegment_sample().
}
\description{
Runs CBS on the log2 ratios produced by read_bam_qc() and returns a
table in the input format expected by resegment_sample().
}
\details{
Requires: DNAcopy (BiocManager::install("DNAcopy"))

CBS is the most widely used segmentation algorithm for detecting
log ratios in copy number data. Implemented in the R package DNAcopy.
}
\examples{
\dontrun{
  qc  <- read_bam_qc("sample.bam", bin_size = 1e6)
  seg <- run_cbs_segmentation(qc, sample_id = "sample_01")
  head(seg)
}

}
